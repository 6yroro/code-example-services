spring:
  application:
    name: data-service

logging:
  level:
    root: info

server:
  port: 9101

eureka:
  client:
#    enabled: false
    service-url:
      default-zone: http://localhost:8761/eureka